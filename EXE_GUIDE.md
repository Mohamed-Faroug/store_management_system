# دليل التحويل إلى ملف EXE

## نظام إدارة المخزون - مخزن الزينة

**جميع الحقوق محفوظة © 2025**  
**تم تطوير هذا النظام بواسطة**: محمد فاروق  
**تاريخ آخر تحديث**: 9/9/2025  

---

## 📋 المتطلبات

### 1. تثبيت Python
```bash
# تحميل Python 3.8+ من python.org
# تأكد من إضافة Python إلى PATH
```

### 2. تثبيت المكتبات المطلوبة
```bash
pip install -r requirements_complete.txt
pip install pyinstaller
```

---

## 🚀 خطوات التحويل

### الخطوة 1: تحضير الملفات
تأكد من وجود الملفات التالية:
```
project/
├── main.py
├── app/
├── requirements_complete.txt
├── README.md
├── backup.py
└── demo_data.py
```

### الخطوة 2: إنشاء ملف spec
```bash
pyi-makespec --onefile --windowed --name="نظام_إدارة_المخزون" main.py
```

### الخطوة 3: تعديل ملف .spec
افتح ملف `نظام_إدارة_المخزون.spec` وتأكد من وجود:
```python
datas=[
    ('app', 'app'),
    ('README.md', '.'),
    ('requirements.txt', '.'),
    ('requirements_complete.txt', '.'),
    ('backup.py', '.'),
    ('demo_data.py', '.'),
],
```

### الخطوة 4: بناء الملف التنفيذي
```bash
pyinstaller نظام_إدارة_المخزون.spec
```

### الخطوة 5: اختبار الملف
```bash
# انتقل إلى مجلد dist
cd dist
# شغل الملف
./نظام_إدارة_المخزون.exe
```

---

## 🔧 تحسينات إضافية

### إخفاء نافذة الأخطاء
```bash
pyinstaller --onefile --windowed --noconsole main.py
```

### تحسين الأداء
```bash
pyinstaller --onefile --windowed --optimize=2 main.py
```

### تضمين أيقونة
```bash
pyinstaller --onefile --windowed --icon=icon.ico main.py
```

---

## 🐛 حل المشاكل الشائعة

### مشكلة: "ModuleNotFoundError"
**الحل**: أضف المكتبة إلى `hiddenimports` في ملف .spec

### مشكلة: "FileNotFoundError"
**الحل**: تأكد من إضافة الملفات إلى `datas` في ملف .spec

### مشكلة: حجم الملف كبير
**الحل**: استخدم `--exclude-module` لاستبعاد المكتبات غير المستخدمة

### مشكلة: بطء التشغيل
**الحل**: استخدم `--onedir` بدلاً من `--onefile`

---

## 📦 توزيع التطبيق

### ملف واحد (OneFile)
- **المميزات**: ملف واحد فقط
- **العيوب**: بطء في التشغيل
- **الاستخدام**: التوزيع السريع

### مجلد واحد (OneDir)
- **المميزات**: سرعة في التشغيل
- **العيوب**: عدة ملفات
- **الاستخدام**: التثبيت الدائم

---

## 🎯 نصائح مهمة

1. **اختبر الملف على أجهزة مختلفة**
2. **استخدم `--clean` لإعادة البناء**
3. **احتفظ بنسخة احتياطية من الكود**
4. **اختبر جميع الوظائف بعد التحويل**

---

## 📞 الدعم

للمساعدة أو الاستفسارات، يرجى مراجعة الكود المصدري أو التواصل مع المطور.

---

**جميع الحقوق محفوظة © 2025**  
**تم تطوير هذا النظام بواسطة**: محمد فاروق  
**تاريخ آخر تحديث**: 9/9/2025
